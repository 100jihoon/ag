#include <stdio.h>

int main() 
{
  int n;
  scanf("%d", &n);
  
  for(int i = n; i>=2; i--)
  {
    if(n % 2 == 0)
      {
        printf("%d - %d - %d = %d\n",i-1, i,i+1, (i-1)-(i)-(i+1));
      }
  else  
  {
    printf("%d + %d + %d = %d\n",i-1, i,i+1, (i-1)+(i)+(i+1));
  }
  }
  return 0;
}
